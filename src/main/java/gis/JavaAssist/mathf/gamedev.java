package gis.JavaAssist.mathf;

public class gamedev {

    // public double t3dDistance(){
    //     return Math.sqrt(Math.pow((), 2)) + Math.sqrt(Math.pow((), 2)) + Math.sqrt(Math.pow((), 2));
    // }
}
